{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "gcc -g -lm -O0 -o a.exe ${file}",
            "problemMatcher": "$tsc", 
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
